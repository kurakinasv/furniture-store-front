export interface Product {
  id: number;
  title: string;
  description: string;
  price: string;
  imageUrl: string;
  imagePlaceholder: string;
}

// todo: обновить типы
export type ProductServer = Product;
